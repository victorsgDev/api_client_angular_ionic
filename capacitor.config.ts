import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'api_client_hito',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
